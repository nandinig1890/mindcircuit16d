---
apiVersion: v1
kind: Service
metadata:
  name: pubg-lb-svc
  labels:
    app: pubg-app

spec:
  type: LoadBalancer
  ports:
    - port: 80                 #browser lo access chese port
      targetPort: 8080         #actual container port
      protocol: TCP

  selector:
    app: pubg-app
    
